#include <iostream>
using namespace std;
int main() {
int n,x;
cout<<"Unesite neki broj n: ";
cin>>n;
cout<<"Unesite neki broj x: ";
cin>>x;
while(x%n!=0)
{
  x++;
}
cout<<"Prvi broj djeljiv brojem n je: "<<x<<endl;
return 0;
}
